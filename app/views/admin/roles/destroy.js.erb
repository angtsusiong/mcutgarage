
<% if @role.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: Role.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: Role.model_name.human) %>', '<%= @role.errors.full_messages.join('\n') %>', 'error');

     <% @role.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#role_<%= message[0].to_s %>" );
    <% end %>
<% end %>
