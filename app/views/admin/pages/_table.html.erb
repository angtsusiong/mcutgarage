<div class="table-responsive">
    <table id='<%= tab_key %>_pages_table' data-tab_column_name='' data-tab_key='<%= tab_key %>' class='table table-bordered table_breaking pages_table m-b-20'>
        <thead>
            <tr>
                <th class='all'><%= Page.human_attribute_name('name') %></th>
                <th class='all'><%= Page.human_attribute_name('desc') %></th>
                <th class='all'><%= t('helpers.datatables.actions') %></th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>
