
<% if @version.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: Version.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: Version.model_name.human) %>', '<%= @version.errors.full_messages.join('\n') %>', 'error');

     <% @version.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#version_<%= message[0].to_s %>" );
    <% end %>
<% end %>
