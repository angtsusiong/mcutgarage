
<% if @badge.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: Badge.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: Badge.model_name.human) %>', '<%= @badge.errors.full_messages.join('\n') %>', 'error');

     <% @badge.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#badge_<%= message[0].to_s %>" );
    <% end %>
<% end %>
