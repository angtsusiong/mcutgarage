
<% if @activity.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: Activity.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: Activity.model_name.human) %>', '<%= @activity.errors.full_messages.join('\n') %>', 'error');

     <% @activity.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#activity_<%= message[0].to_s %>" );
    <% end %>
<% end %>
