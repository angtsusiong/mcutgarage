$('#master_show_tab').val('<%= @current_tab[:name] %>');
tab_html = '<%= j render "admin/project_categories/tabs/#{@current_tab[:name]}_tab", project_category: @project_category, master_show_tab: @current_tab[:name] %>';
if(tab_html !== '') {
    $('.tab-pane.active').removeClass('active');
    $('.tab.active').removeClass('active');
    $('#<%= @current_tab[:name] %>_tab').addClass('active');
    $('#<%= @current_tab[:name] %>_tabpanel').addClass('active').html($(tab_html));
}
