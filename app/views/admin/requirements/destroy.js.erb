
<% if @requirement.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: Requirement.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: Requirement.model_name.human) %>', '<%= @requirement.errors.full_messages.join('\n') %>', 'error');

     <% @requirement.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#requirement_<%= message[0].to_s %>" );
    <% end %>
<% end %>
