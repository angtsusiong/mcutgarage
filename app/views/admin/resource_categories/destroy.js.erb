
<% if @resource_category.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: ResourceCategory.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: ResourceCategory.model_name.human) %>', '<%= @resource_category.errors.full_messages.join('\n') %>', 'error');

     <% @resource_category.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#resource_category_<%= message[0].to_s %>" );
    <% end %>
<% end %>
