
<% if @related_resource.errors.full_messages.blank? %>
    swal('<%= t('helpers.form.destroy_success', model: RelatedResource.model_name.human) %>', '', 'success');
<% else %>
    $( "span.full_error" ).remove();

    swal('<%= t('helpers.form.destroy_error', model: RelatedResource.model_name.human) %>', '<%= @related_resource.errors.full_messages.join('\n') %>', 'error');

     <% @related_resource.errors.messages.each do |message| %>
        $( "<span class='error full_error'><%= message[1].join('; ') %></span>" ).insertAfter( "#related_resource_<%= message[0].to_s %>" );
    <% end %>
<% end %>
